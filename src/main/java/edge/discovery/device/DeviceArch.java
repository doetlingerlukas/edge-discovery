package edge.discovery.device;

/**
 * Enum for different architectures discovered devices may have.
 *
 * @author Lukas Dötlinger
 */
public enum DeviceArch {
  X86,
  X86_64,
  ARM,
  AARCH64,
  MIPS,
  MIPS64,
  POWERPC,
  POWERPC64,
  RISCV64,
  S390X,
  SPARC64
}
